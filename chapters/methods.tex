
\chapter{Methods and Setup} \label{chap:methods and Setup}



In this chapter, the methods employed in this thesis are provided. 
First, we present the employed simulation methods.
Afterwards the simulations setups are introduced and finally the methodology to derive and employ \acrshort{zpg} conditions is explained. 

\section{Simulation methods}\label{sec:SimMethods}
\fxbmark{Korekturbeginn}
In this section, the numerical methods utilised in this work are introduced.
Firstly, we focus on \acrfull{dns} which we primarily use throughout this work.
Thereafter, \acrfull{rans} are presented, as additional simulations are performed with this method.
\fxmarlenebmark{ab hier Sachen hinzugefügt 19.07. 11:40}
\subsection{Direct numerical simulation in Neko}\label{subsec:DNSmethod}

For the simulation of turbulent flows a range of methodologies can be employed.
The decision regarding the utilisation of a specific approach  depends on the characteristics of the flow and the requirements for the solution.
One straight-forward approach is the \acrshort{dns}.
\acrshort{dns} necessitates the solution of the \acrshort{nse} in order to compute  $U(x,t)$.
Thus, \acrshort{dns} is the computationally most expensive approach, given that it resolves all length and time scales \citep{pope_turbulent_2015}. 
Conversely, this approach yields  the most accurate results.
In the context of small turbulent scales, the utilisation of \acrshort{dns} is inevitable.
Due to the high computational demands, \acrshort{dns} is typically employed in the study of low to moderate Reynolds numbers. 
Its application is primarily restricted to  research contexts \citep{slotnick_cfd_nodate}.
The simulation method of \acrshort{dns} is the principal approach employed in this project.

%Developing this project involved the utilisation of a range of tools. 
%In this project, the majority of fluid simulations were performed with \acrshort{dns}. 
To execute these simulations the spectral element code \name{neko} was used. 
\name{neko} is an open-source code developed for \acrshort{dns} and \acrshort{les} simulations of incompressible turbulent flows.
It is the direct decendant of \name{nek5000} which is a well-established \acrfull{cfd} implementation.
\name{neko} is written in modern, object-oriented Fortran code \citep{jansson_neko_2024}.
It solves the \acrshort{nse} based on the high-order \acrfull{sem} which was first introduced by \citet{patera_spectral_1984}.
In \acrshort{sem} the methods of \acrshort{fem} and spectral methods are combined \citep{patera_spectral_1984}.
Therefore, the domain is divided into small elements adding up to a mesh structure that represents the domain similar as in \acrshort{fem}.
This enables the representation of complex geometries through various element shapes and arrangements.
Additionally, the resolution of the solution can be adjusted by varying the size of the elements.
In \acrshort{fem}, the quantities inside the cells are  usually calculated with low-order approximations.
Therefore, a refinement of the mesh would cause an algebraic error reduction \citep{deville_high-order_2002}. 
In order to achieve well-resolved results, it is necessary to employ meshes with extremely high element numbers. This, in turn, increases the  computational costs significantly.
Hence, the spectral method was introduced by \citep{patera_spectral_1984}.
The idea of the spectral method is to use a single high-order polynomial to represent the behaviour of the complete domain.
With this approach, the error decreases exponentially with increasing polynomial order \citep{deville_high-order_2002}.
However, the spectral method is only applicable to quite simple geometries since a single polynomial cannot match complex geometries.
Therefore, the approaches are combined in the following way:
The domain is discretised in elements according to \acrshort{fem} and the solution within one element is approximated with a high-order polynomial as in the spectral method.
The high-order polynomial can be expressed as a tensor-product of 1D Lagrange interpolating polynomials \citep{deville_high-order_2002}.
Inside a single element, the polynomial is defined through a set of points.
These \benennung{\acrfull{gll}} points have a specific non-uniform distribution across the element which is visualised in \autoref{fig:gllPoints}.
Their spacing is more compact near an element boundaries,  thereby mitigating oscillation issues associated with high-order polynomials \citep{deville_high-order_2002}.
In \autoref{fig:gllPoints}, it is also visualised how the positions of the \acrshort{gll} points are adopted if the shape of the element is modified.
This could happen through complex domain shapes, where the element shape needs to be adapted to the shape of the domain boundaries as in the curved domain studied in this project.
Since a \acrshort{gll} point is positioned at the boundaries of the element also, the number of \acrshort{gll} points is $N+1$ for representing a polynomial of order $N$.
The \acrshort{gll} points at the element boundaries ensure the continuity of the solution across element boundaries \citep{deville_high-order_2002}.
Usually, different polynomial orders are used to represent velocity and pressure field to avoid spurious pressure oscillations \citep{maday_spectral_1989}.

\begin{figure}[H]
    \centering
    \input{images/gll_points.pgf}
    \caption[GLL points on a curved spectral element]{%
    Distribution of the \gllmark~\acrshort{gll} points within a single
    spectral element of polynomial order $N=6$ in two-dimensional space (a) Reference element with quadratic shape; (b) shape modified element to match a curved domain shape.}
    \label{fig:gllPoints}
\end{figure}
\fxchange{Das klingt nach einer guten Erklärung! vllt noch 2 Sätze, die ausdrücken, dass das ein aussichtsreicher Kompromiss zwischen reduziertem computationel effort and the required accuracy bei der Bestimmung von xx sein könnte (weil das bei iEinem anderen Anwendungsgebiet auch schon mal probiert wurde, oder weil yyy oder ohne weil - ist halt Forschung)}
\fxmarlenebmark{bis hier Sachen hinzugefügt 19.07. 11:40}
\fxbmark{fertig}
\comment{
When running simulations with \name{neko}, it is possible to track statistics regarding the simulated flow. 
The recording of the statistics can be configured in various manners and is defined in the so-called case file of the simulation.
For instance, it is possible to define one or two axes along which the statistics should be averaged. 
These statistics are used to extract and calculate certain metrics of the flow. In order to accomplish this, it is necessary to undertake a process of post-processing of the recorded statistics. 
For this purpose,  the \name{python} package \texttt{pysemtools} is employed. 
\name{pysemtools} facilitates the extraction and interpolation of data, like those resulting from \name{neko} simulations, from an SEM mesh into any arbitrary points \fxchange{ Quelle:GitHub repository}. 
Subsequently, a multitude of statistical metrics, including budget terms, can be calculated. 
}


\subsection{RANS simulations in OpenFOAM}\label{sec:RANSmethod}
%In addition to the DNS, a number of \acrshort{rans} simulations were conducted as well. 

In the course of this work, we also employed \acrshort{rans} simulations.
They are based on the already introduced Reynolds decomposition of the \acrshort{nse} (see \autoref{subsec:TBLTheory}).
The resulting equations for \acrshort{tbl} are written in \autoref{eq:rans} which enables the computation of the mean pressure and velocity fields.
The fluctuations are represented in the Reynolds stress tensor. 
In order to circumvent the full computation of this tensor, a number of methodologies have been formulated for its approximative modelling \citep{wilcox_turbulence_2006}. 
Since the models differ with respect to characteristics and accuracies, the selection of the most appropriate model is of paramount importance, as it will have a substantial impact on the simulation \citep{yang_assessment_2016}.
Within the simulation, we employ the so-called $k\text{-}\omega$ SST model.
This model combines the $k\text{-}\omega$ and $k\text{-}\varepsilon$ models, incorporating an additional term for the shear-stress transport (SST) \citep{menter_two-equation_1994}:
In the sublayer of the boundary layer, the $k\text{-}\omega$ model is used due to its proven accuracy in this region. 
The $k\text{-}\omega$ model is then blended with the $k\text{-}\varepsilon$ model, which has been shown to provide more accurate results for $k$ and $\varepsilon$.
These quantities determine the eddy viscosity which is the primary information required by the mean flow solver. 
Therefore, an accurate prediction of mean flow depends on accurate values for $k$ and $\varepsilon$ \citep{menter_two-equation_1994}.
It is evident that these simulations exhibits superior computational  performance in comparison to \acrshort{dns}.
In case the detailed behaviour of the turbulent scales is irrelevant, and the focus is on the effect of the turbulence on the flow, \acrshort{rans} simulations provide a reasonable compromise \citep{alfonsi_reynolds-averaged_2009}.
Consequently, this simulation method finds application in multiple industrial environments.


\begin{figure}[H]
    \centering
    \input{images/fvm_stencil.pgf}
    \caption[Finite-volume stencil]{%
    Finite-volume discretisation: a $3\times3$  mesh
    around the control volume $P$ with its neighbours $N$, $S$, $E$, $W$ and
    the face centres $n$, $s$, $e$, $w$.
    \centermark~cell centres store the flow variables (collocated arrangement);
    \facemark~face centres are where interpolated values are evaluated.
    Double-headed arrows across the faces of $P$ indicate the conservative flux
    exchange with the neighbouring cells.}
    \label{fig:FVMMethod}
\end{figure}

In order to execute these simulations, the open-source software \name{OpenFOAM} is utilised. 
This software is able to perform \acrshort{rans} simulations of fluid flows with a variety of turbulence models. 
\name{OpenFOAM} implements the \acrfull{fvm} to approximate the \acrshort{nse} \citep{weller_tensorial_1998}.
The numerical approximation of the \acrshort{nse} is achieved by the subdivision of the domain into finite control volumes. 
This approach stands in contrast to the \acrshort{dns}, wherein the equations are solved analytically.
For each of these cells, the equations are then integrated over the volume, thereby converting them into a sum of fluxes through the cell edges.
This property makes the method inherently conservative, since all field quantities, e.g.~pressure and velocity components, that leave a cell across an edge enter the neighbouring cell \citep{moukalled_finite_2016}. 
Therefore, mass and momentum are exactly conserved in the discretisation \citep{weller_tensorial_1998}.
Most codes are based on a so-called collocated variable arrangement, meaning that all quantities are stored at the same point in the control volume, e.g., the cell centre.
However, it is necessary to derive the field quantities at the cell edges to determine the fluxes across the cell edges \citep{schlatter_lecture_2025}.
This is usually done by interpolation of the field quantities which is visualised in \autoref{fig:FVMMethod}
A range of schemes exist for the purpose of performing this interpolation. 
Specific interpolation schemes can be utilised for individual  quantities depending on their properties.
In \name{OpenFOAM} the interpolation schemes for the different variables are defined in the \texttt{fvSchemes} file.


Using the previously described approach, most variables can be successfully quantified.
However, for incompressible flows it is not possible to derive the pressure  directly through with this method.
The pressure manifests exclusively in a derivative, thus there is no explicit term to calculate the pressure.
Nevertheless, the prediction of the pressure  must be conducted in a manner that satisfies  the following condition:

If the predicted solution for the pressure is included in the momentum equation, the velocity values are resolved in such a way that they fulfil the continuity equation.
In order to achieve this objective, a  number of  methodologies can be employed
 to derive a pressure field that satisfies this condition with a \benennung{predictor-corrector iteration} \citep{schlatter_lecture_2025, weller_tensorial_1998}.
One  commonly used  approach is the \acrfull{simple} approach, which is also implemented in \name{OpenFOAM}.
It consists of four basic steps \citep{schlatter_lecture_2025}:
\begin{enumerate}
    \item Prediction: Estimate the pressure field and solve the momentum equations.
    \item Poisson equation: Solve the additional pressure equation for the  pressure correction variable $p'$.
    \item Correction: Correct the pressure and the velocity fields with the pressure correction variable to improve the mass conservation.
    \item Repetition: update the initial values for  pressure and velocities and repeat until a converged solution is obtained.
\end{enumerate}

%In addition to the method for the pressure prediction, the turbulence model has to b



%The OpenFOAM software is used to perform the RANS simulations for optimisation.
%As described in \autoref{sec:SimBasics} \acrshort{rans} simulations use turbulence models, whereas \acrshort{dns} simulate the turbulence completely.

%There are various commonly used models for describing the turbulence, which have different properties and accuracies.



 

%All simulations are performed on the cluster of the RRZE of the Friedrich-Alexander-Universität Erlangen-Nürnberg (FAU).  \fxchange{Blabla über cluster}


\section{Simulation Setups}\label{sec:MethodCurvedTBL}

In the following section the simulation setups are presented.
First we introduce the setup we use for the \acrshort{dns} simulations.
Then the simulation setup for the \acrshort{rans} simulations is presented.

\subsection{Neko simulation setup}

In order to derive the curved \acrshort{zpg} \acrshort{tbl}, we first need to create a general setup for the curved \acrshort{tbl}. 
The initial configuration of the plane  \acrshort{tbl} is utilised as the basis, and we add a curved part at the end of the domain of the plane \acrshort{tbl}. 
The setup of the plane \acrshort{tbl} is described in \autoref{sec:planeTBL}.
It is imperative to consider the plane part, as this is necessary for the optimal development of the \acrshort{tbl}. The objective is to achieve
a value of $Re_{\theta} \approx 1000$ when the curvature starts. 
The curved part has a constant radius and develops in negative wall-normal direction. 
Therefore, the \acrshort{tbl} experiences a convex curvature. 
At the end of the curvature, an additional straight domain segment is appended, which is called \benennung{\acrlong{so}}.
A seamless transition is observed between the curved segment and the straight one \acrlong{so}, as the former aligns with the tangential direction of the curved segment. 

The complete setup is shown in \autoref{fig:paraviewDomain}.
The \acrlong{so} is introduced to reduce the impact of the \acrlong{bc} at the outflow. 
The pressure \acrshort{bc} at the outflow forces the pressure to be zero.
Consequently, the pressure trend in front of the outflow is affected by the specified boundary condition.
In order to circumvent  that the \acrshort{bc} impacts the pressure progression in the curvature, the \acrlong{so} is added.
The mesh is generated in a manner analogous to that employed for the plane  \acrshort{tbl} with the addition of the curved part and the \acrlong{so}.
The curved segment is created with a circular arc with the corresponding values for the radius and angle.
In the \acrlong{so} the mesh is coarsened since this part of the domain is not relevant for the study of the curved \acrshort{tbl}. \\

\subsubsection{Boundary and initial conditions}
The imposed boundary and initial conditions are similar to those of the plane \acrshort{tbl} simulations.
The initial condition is a \benennung{Blasius profile}, as in the plane \acrshort{tbl} simulations (see \autoref{sec:planeTBL}).

\comment{hier blasius profile beschreiben}
The Blasius profile is an analytic solution for the velocity profile of a laminar boundary layer along a flat plate with zero pressure gradient \citep{blasius_grenzschichtern_1908}.
It is based on the self-similar character of the boundary layer meaning the velocity profile along the flat plate looks similar at any streamwise position once it is rescaled with the local boundary layer thickness \citep{schlichting_boundary-layer_2017}.
The outflow condition and the lower wall boundary condition are also transferred.
A \benennung{Dirichlet condition} is applied as boundary condition for the top wall.
Since the domain is curved the coordinate system needs to be adapted.
We introduce a moving coordinate system that is based on the streamwise direction $s$, the wall-normal direction $n$ and the spanwise direction $z$.
The corresponding velocities are named as $u$, $v$ and $w$.
Hence, the Dirichlet condition with the velocity components $(1,0,0)$ is applied in the new coordinate system.
In \autoref{sec:planeTBL}, normal outflow and the Dirichlet  boundary condition on the top wall are compared  in terms of their effect on the \acrshort{tbl} development.
These validations show that, ultimately, the differences on a flat \acrshort{tbl} are negligible, meaning that the Dirichlet boundary condition does not prevent the growth of the \acrshort{tbl}.
Therefore, we transferred this finding to the curved \acrshort{tbl}, which also simplifies the scenario setup.
The Dirichlet condition can be applied in the \texttt{f90} file, where the normal and tangential velocity components can be calculated and set as the user velocity. 
The normal outflow condition has been  implemented in neko, but currently only works for normal directions axis-aligned to a fixed $xyz$-coordinate system. 
The Blasius boundary layer was used as the initial condition similar to the plane \acrshort{tbl}.
In the curved part the Blasius profile is rotated so that it builds up in the wall-normal direction. \\

\begin{table}[!ht]
    \centering
    \caption{Overview of the tested curved TBL cases}
    \label{tab:curved_cases}
    \begin{tabular}{l l c c c}
    \hline
    \textbf{Name} & & \textbf{R100deg30} & \textbf{R100deg90} & \textbf{R500deg18} \\
    \hline
    \multicolumn{2}{l}{\textbf{Angle}} & $30$° & $90$° & $18$° \\
    \hline
    \multirow{2}{*}{\textbf{Radius}} & $\times \delta_{99}$ & $100$ & $100$ & $500$ \\
     & $r$ & 610 & 610 & 3050 \\
    \hline
    \multirow{2}{*}{\textbf{Straight inlet}} & $\times \delta_{99}$ & $53$ & $53$ & $53$ \\
     & $s_{i}$ & 325 & 325 & 325 \\
    \hline
    \multirow{2}{*}{\textbf{Arc length}} & $\times \delta_{99}$ & $53$ & $157$ & $157$ \\
     & $s_c$ & 319 & 958 & 958 \\
    \hline
    \multirow{2}{*}{\textbf{Straight outlet}} & $\times \delta_{99}$ & $27$ & $27$ & $53$ \\
     & $s_o$ & 162 & 162 & 325 \\
    \hline
    \multirow{2}{*}{\textbf{Wall-normal }} & $\times \delta_{99}$ & $4$ & $4$ & $8$ \\
     & $n$ & 162 & 162 & 325 \\
    \hline
    \multirow{2}{*}{\textbf{Spanwise }} & $\times \delta_{99}$ & $4$ & $4$ & $6$ \\
     & $z$ & 162 & 162 & 325 \\
     \hline
    \end{tabular}
\end{table}

This setup was used to create three different variations of the curved \acrshort{tbl}.
We tested two different radii to study the impact of varying curvatures. 
The radii used were  $R_1 = 100 \cdot \delta_{99}$ and $R_2 = 500 \cdot \delta_{99}$ with $\delta_{99}$ measured in the plane \acrshort{tbl} at $Re_{\theta} = 1000$. 
Two setups were tested with radius $R_1$ namely at $30$° and $90$°. 
For the second, only one setup was tested, with $18$° curvature. 
This value was chosen because it has the same arc length as the case with radius $R_1$ and $90$° curvature. 
Therefore, the cases have a comparable curved section lengths.
The domain dimensions for the tested cases are summarised in \autoref{tab:curved_cases}. 
We used these simulations as references to evaluate the effect of pressure correction on the simulation and its statistics.
Therefore, all configurations were simulated once with a $(1,0,0)$ and an optimised velocity profile on the top wall.
Additionally, these simulations were used to extract the inflow conditions for the \acrshort{rans} simulations.

\comment{
\begin{table}[!ht]
    \centering
    \caption{Overview of the tested curved TBL cases}
    \label{tab:curved_cases}
    \begin{tabular}{ c c c c c c c c c c}
    \hline
    \textbf{Name} & \textbf{Angle}  & \multicolumn{2}{c}{\textbf{Radius}} & \multicolumn{2}{c}{\textbf{Straight length}} & \multicolumn{2}{c}{\textbf{Arc length}} & \multicolumn{2}{c}{\textbf{Tail length}}\\
    && $\times \delta_{99}$ & physical  & $\times \delta_{99}$ & physical & $\times \delta_{99}$ & physical&  $\times \delta_{99}$ & physical \\
    \hline
    R100deg30 & $30$° & $100$ & 610  & $53$ & 325 & $53$ & 319& $27$ & 162\\ 
    R100deg90  &$90$° & $100$ & 610 & $53$ & 325 & $157$ & 958&$27$ & 162\\ 
    R500deg18 &$18$° & $500$ & 3050  & $53$ & 325 & $157$ & 958&$53$ & 325\\ 
    \hline
    \end{tabular}
\end{table}
}



\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{curvedDomain_IC.pdf}
    \caption{This picture will be recreated}
    \label{fig:paraviewDomain}
\end{figure}

\subsection{OpenFOAM simulation setup}\label{subsec: methodsOFsetup}


\begin{figure}[H]
    \centering
    \input{images/domain_overview.pgf}
    \caption[XX]{Constitution of the OpenFOAM domain for the case R100deg30 with examplary positioning of the \benennung{design points (DP)}:
    \lswatch{wallcol}~ fixed bottom wall, inlet top wall, outlet top wall;
    \dswatch{cpdot}~movable \benennung{design points} with \lswatch{rangecol} their design space;
    \lswatch{splinecol}~upper wall shape defined by the position of the DP;
    \lswatch[dashed]{neutralcol} neutral undeformed position of the upper wall;
    \lswatch[dashed]{onsetcol} limits of the curved section.
}
    \label{fig:openfoamDomain}
\end{figure}

\comment{
    \lswatch{splinecol} current (deflected) shape relative to its
    \lswatch[dashed]{neutralcol} neutral, undeformed position.
    \lswatch[dashed]{onsetcol} mark the onset of curvature and the tail start.
    The \dswatch{cpdot} spline control points move within their
    \lswatch{rangecol} admissible range (drawn exaggerated for clarity).
}

\subsubsection{Mesh generation}
Transferring the curved TBL simulation setup to \name{OpenFOAM} requires some adaptations which will be presented here. 
First, the mesh has to be regenerated. 
It is not possible to use a mesh created in \name{gmsh} and transfer it to \name{OpenFOAM}, since the code must adapt the domain and the mesh after each iteration of the optimisation.
Therefore, the domain has to be regenerated with \verb|blockMesh| in every iteration.
As a consequence, the domain has to be defined in the \verb|blockMeshDict|.
To ensure that the results from the \name{OpenFOAM} simulation can be transferred into \name{neko}, the setup must be quite similar.
Therefore the domain has the same curvature as well as  inlet and outlet regions similar to the constitution of the \acrshort{dns} domain.
However, the inlet is shorter than the one in the \acrshort{dns} domain.
The initial condition for the \name{OpenFOAM} simulation is extracted at a position $s_{extr} = 275$ in the straight section of the \acrshort{dns} domain.
Therefore, the boundary layer in the \acrshort{rans} simulation only needs a much shorter straight inlet region to develop similar to the \acrshort{dns} boundary layer.
We defined the remaining length of the straight inlet as the remaining part of $s_{i} - s_{extr}$ and then added a balance zone of $s_{bal} = 20$ which ends up in a straight inlet of $s_{iOF} = 70$.
This balance region is recommended since quantities of the extracted \acrshort{dns} inflow profile have to be rebalanced in the \acrshort{rans} model \fxinfo{(XXQUELLE)}.
In \autoref{fig:openfoamDomain} the general configuration of the \acrshort{rans} domain is depicted exemplary for the R100deg30 case.
Compared to the domain used for the DNS, the height of the domain is increased to $10 \cdot \delta_{99}$ for R100deg30 and R100deg90, and up to $20 \cdot \delta_{99}$ in R500deg18.
This modification simplifies the transfer of the results of the optimised \name{OpenFOAM} simulation to the DNS domain, as  described in \autoref{subsec:data_extr}.
To enable the adaption of the \benennung{design points}, the upper wall cannot be defined as a fixed circular arc in the curved section.
Instead, it is defined as a spline defined by the \benennung{design points}.
The coordinates $x_{\theta_i}$ and $y_{\theta_i}$  describe the position of the \benennung{design points} $\theta_i$ which are modified to minimise the \benennung{objective function}.  
The optimisation code is constructed in a way that it modifies the position of the \benennung{design points} in the wall-normal direction. 


Since the domain is curved, the wall-normal direction does not correspond to a fixed axis. 
Therefore, the \benennung{design points} $\theta_i$ must be transformed into  $x-$ and $y-$ coordinates to define the spline correctly.
This will be discussed in detail in \autoref{subsec:opt_settings}.
The lower wall is defined first as a straight edge and then transitions into a circular arc.
The straight outlet is not part of the spline, since there no optimisation is required.
It is meshed as a block with straight vertices.
The mesh resolution increases near the wall to ensure $y^+ = 1$ near the lower wall.


%To match the lower wall to this configuration we defined it also as spline but increased the number on control points to a very high level, such that it is quite similar to an analytical circular arc.




\subsubsection{Boundary and initial conditions}
The quantities that require predefined initial conditions depend on the selected RANS model \fxinfo{(XXQuelle)}. 
Since  the $k-\omega-sst$-model is used in this project, it is necessary to define the initial conditions for turbulent kinetic energy $k$, dissipation $\omega$ and velocity $U$. 
This is achieved by applying an initial field at the inflow which is extracted from the previous \acrshort{dns} simulation of the curved \acrshort{tbl}.
Therefore, a slice of the fully developed \acrshort{tbl} from the DNS simulation is used.
We take this slice at the streamwise position in the straight inlet where $Re_{theta} = 1000$ is reached.
At this streamwise position the fields of $U$, $k$ and $\omega$ are extracted across the entire height of the domain. 
In the spanwise direction the fields are already averaged which corresponds to the two-dimensionality of the \name{OpenFOAM} simulation.
In order to do this we perform a post-processing of the \acrshort{dns} simulation using the \name{OpenFOAM} mesh as interpolation points.
With the \name{Python} package \texttt{fluidfoam} it is possible to read the points from an \name{OpenFOAM} mesh into \name{Python}. 
This is used to extract the points of the \name{OpenFOAM} mesh at its inflow.
These points are used as interpolation points to perform the post-processing. 
As described earlier the domain used in \name{OpenFOAM} is higher than the one for the \acrshort{dns} simulations.
The usage of this higher domain would lead to interpolation conflicts since there are no data for the upper interpolation points. 
To avoid these conflicts the interpolation points lying beyond the height of the \acrshort{dns} domain are cut.
They will be filled manually at the end of the script.
The values for $U$ can be extracted directly, whereas $k$ and $\omega$ need to be computed. 
To use these fields in the higher \name{OpenFOAM} domain additional values for the upper part of the inflow are manually added. 
For this part of the inflow the last interpolated value is copied and added in the remaining cells.

\section{Optimisation method}

This section introduces the procedure for generating a \acrshort{zpg} in the curved \acrshort{tbl} domain. 
This procedure is performed using \name{OpenFOAM} and the Bayesian optimiser code developed by \citet{morita_applying_2022}. 
As described in \autoref{sec:OptimsationBasics}, the code is able to optimise the shape of the domain to achieve a given Clauser parameter,  in this case the parameter is a ZPG. 
To achieve this, the simulation setup has to be transferred to \name{OpenFOAM}. 
In the following paragraph the necessary steps are described in detail. 

In the following subsection the general workflow of the optimisation algorithm is outlined.
Then, the positioning of the \benennung{control points} is explained and additonal settings for the optimisation are presented.


\subsection{Optimisation algorithm} \label{subsec:opt_settings}




\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{BOGP_flowchart_horizontal.pdf}
    \caption{Schematic visualisation of the workflow of the optimisation algorithm developed by \citet{morita_applying_2022}}
    \label{fig:optiFlowchart}
\end{figure}


The optimisation algorithm is based on a Bayesian optimisation with \acrfull{gpr}.
\fxinfo{hier noch merh zur optimierer theorie}
The algorithm was first introduced by \citet{morita_applying_2022} and tested for different cases by \citet{ganagdhar_bayesian_2025}.
The workflow of the algorithm is depicted in \autoref{fig:optiFlowchart} and can be split into 6 steps.
First, the next samples for $\theta_i$ are proposed by the \acrshort{gpr}.
The first iteration are randomly taken samples. 
Then, the \acrshort{gpr} suggests the next combination based on all previous iterations.
They are transferred into the samples into $x_{\theta_i}$ and $y_{\theta_i}$ to include them into the \texttt{blockMeshDict}.
In the next step the \name{OpenFOAM} simulation starts which consists of three steps.
First the new mesh is generated, then the case is split to the processors and then the simulation is run.
Once the \name{OpenFOAM} simulations ends, multiple boundary layer quantities are calculated to determine the Clauser parameter. 
With this, the residual, which is defined by the normalised $L^2$-norm of $\beta(s)$, is calculated as
\begin{equation}
    R = \frac{\lVert \beta_{\text{eval}} - \beta_{\text{target}} \rVert_2}{\sqrt{N_{\text{eval}}}}
    \label{eq:residual}
\end{equation}

with $\beta_{eval}$ representing the calculated value in the current iteration.
$\beta_{target}$ is the objective value. 
The $L^2$-norm is normalised by the number of cells that are included in the evaluation window $N_eval$.
That is necessary for comparisons between residuals of different cases and respectively different evaluation window sizes.
In the next step the setup of the \texttt{OFcase} is stored if the residual is below a user-defined threshold.
That is good to later compare different configurations with comparable good residuals.
In the last step the \benennung{design points} are added to the \texttt{gpList.dat} and the convergence check is performed.
These steps are repeated until the residual is converged or the maximum number of iterations is reached.

\subsection{Optimisation setup}

\subsubsection{Positioning of the design points}

\begin{figure}[H]
    \centering
    \input{images/domain_cps.pgf}
    \caption[Positioning procedure of \benennung{design points} for the Bayesian optimisation]{Positioning procedure of \benennung{design points (DP)} for the Bayesian optimisation;
    First positioning: \dswatch{cpone}~5 DP in the curvature,
    Second positioning: \dswatch{cptwo}~3 DP in the curvature,
    Third positioning: \dswatch{cpthree}~2 DP in the straight inlet;
    \lswatch{splinecol} spline defined by the design points;
    \lswatch{targetcol} evaluation window on the lower wall;
    \lswatch[dashed]{onsetcol} limit of the curved segment.
    }
    \label{fig:OF_designPOints}
\end{figure}


The general settings for the optimisation are the same for all tests performed.
However, the number and position of the \benennung{design parameters} varies depending on the case.
In \autoref{fig:OF_designPOints} the positioning procedure of the \benennung{control points} is visualised.
In all cases, optimisation was started with five \benennung{design points} uniformly distributed along the curvature.  
Their position is marked in \autoref{fig:OF_designPOints} as 'Positioning 1'.
For the R100deg30 case this setup produced insufficient small residuals. 
Therefore  more \benennung{design points} were successively positioned  between the existing ones,  where the Clauser parameter showed high variations.
They are visualised in 'Positioning 2' in \autoref{fig:OF_designPOints}.
In the final configuration, we used eight \benennung{design parameters} in the curved area for this case.
Optimisation of this setup revealed a sharp \acrshort{fpg} spike at the start of the curvature, not yet being controlled by the optimisation.
Since we wanted to enforce a \acrshort{zpg} already at the start of the curvature, we needed to modify the optimisation process.
First, the current best setup was copied and its \benennung{design parameters} stored. 
In the new optimisation setup, the focus is on the part near the start of the curvature only.
In order to manipulate the pressure gradient in this region, it was necessary to position additional \benennung{design parameters} in the straight part. 
We decided to add two more \benennung{design parameters}.\fxinfo{Gibt es nen Grund dafür?}
One of these is positioned directly at the transition from the straight to the curved segment.
The second one is positioned at $\frac{2}{3}s_p$ behind the inflow.
These additional points extend the optimisation range along the lower wall beyond the beginning of the curvature which is depicted in green in \autoref{fig:OF_designPOints}. 
Since the length of this inflow region is quite small relative to the total  length of the optimisation  along the curvature, adding these points without changing the optimisation settings would not significantly affect the progression of $\beta$.

This is because this region only has a low impact on the mean value of $\beta$, which is the only value optimised.
Additionally, this would\fxunsure{verstehe ich nicht: Gerade wurde doch gesagt, dass diese 2 Punkte jetzt dazugenommen werden, jetzt doch nicht - fehlt hier ein Satz was man sonst so machen würde??} increase the optimisation dimension by two, which would significantly slow down the optimisation process.
Therefore, we chose a different method. 
Since the optimisation performed for the curved segment only already produced quite good results, we adopted the optimised design parameters from there in the new setup. 
We fixed the six downstream parameters of these eight parameters since they were already it a good position.
Moreover, we assumed that the inflow modification  would not significantly impact the pressure field near the end of the curvature.
Thus, we only have two free \benennung{design points} in the curvature and two in the straight section in front of it which fastens the optimisation.
In addition, we applied a weight to the transition section of the optimisation length to increase the impact of the new design points.
We tested multiple other setups and distributions of the \benennung{design points} to ensure an accurate result.
The results of the different configurations are discussed in chapter 5.
For the other two cases we decided to directly follow the best working principle to position the \benennung{design variables} directly.

\comment{
XXX shorten optimisation lengthXX??
Thsi new setup then lead to the beta progression shown in figure XX.
here we can see that the neegaitve spike at hte beginning of the curvature already recude significantly compered to the previous figure.
However it did not ddisappear completely.
XX blabl warum??


In the first configuration we distributed the points uniform along the spline from $s=43$ to the end of the curvature as depicted in fig XX.
In addition, we tested a sensible distribution of the points.
To do this we first performed some previous tests to identify the most sensible areas along the spline, and then seek the \benennung{design points} there.
The optimisation of this setup then leads to the progression of beta along the lower wall pictured in figure X.
As we can see there the value of beta XXXX.
HOwever, there is a sharp negative peak when the curvature starts.
This means that with this set up the inflow in the curved part odes not fulfill a \acrshort{zpg}.
}
As mentioned previously, we started with five \benennung{design points} in all cases.
For the R500deg18, this resulted in small residuals.
This can be explained by the lower curvature in this case which reduces the natural pressure gradient caused by the curvature.
Therefore, the necessary compensation is much smaller. 
Additionally, the longer arc length provides more space for pressure changes, resulting in smaller gradients along the domain \fxinfo{(XXQUELLE)}.
However, the R500deg18 case also shows a \acrshort{fpg} spike in the curve of the Clauser parameter.
Therefore, as with R100deg30, we added  two \benennung{design points} in the straight inlet. 
We fixed the last four out of five points in the curved section reducing the optimisation problem to three dimensions with only the upstream points  free.
With this configuration, it was possible to achieve a fairly good optimisation of the $\beta$ distribution.


\fxchange{For R100deg90 XXXX}

\comment{
In all cases we started these two configurations with 5 \benennung{design points}.
Depending on the results we decided whether we needed more control points to further reduce the residual of the optimisation.
For the three different cases we tested we ended up with different numbers of control points.
Because of the varying geometry of the cases, the capability of the optimisation algorithm varied.
XX blabla concret für cases


In Figure XX we see the progression of the clauser parameter along the streamwise direction s. (XX in figure vergleich zw optimiertem und unoptimiertem verlauf?)
We can see that at the start of the curvature beta has a significantly high negative spike in the reference case.
With the optimisation this spike is reduced.
However with the sharp change from traight to curved wall, it will not be possible to compensate the spike completely.
}




\subsubsection{Optimisation parameters}

Besides the positioning of the \benennung{design points} other parameters have to be determined for the optimisation algorithm.
For simplicity, all user-defined parameters are defined in a single \texttt{params.py} file.
There e.g. specific dimensions for the geometry of the domain can be defined, such as the radius, the length of the straight inlet and outlet and the angle.
The \texttt{blockMeshDict} such that the general configuration of the domain with straight inlet, curved section and straight outlet is predefined.
In addition, the position of the \benennung{control points} in streamwise direction can be defined.
Regarding the \benennung{design points}, the design space also has to be limited for all \benennung{control points}.
We adapt their \benennung{design space} iteratively starting with the same wide range for all \benennung{control points}.
During the optimisation process we observe the motion of each control points in its range.
If \benennung{control points} tend over multiple iterations to a limit of their \benennung{design space}, we interrupt the optimisaiton and adapt their range correspondingly.
To avoid a steady extension of the \benennung{design space}, the opposite limit is also changed to keep the \benennung{design space} size constant.
The optimisation then can be restarted, and the old optimisation results are still taken into account from the \acrshort{bo}.
However, these adjustments require caution as hasty modifications can distort the exploration of the \acrshort{bo}.
Therefore, these adjustments should only be made if there is a long-term trend.
Concerning the optimisation algorithm, the evaluation window at the lower wall has to be defined.
As visualised in \autoref{fig:OF_designPOints} the evaluation window does not cover the complete bottom wall of the domain.
Instead the evaluation window is confined to a section on which the modification of the \benennung{design points} has the capacity to exert an effect on $\beta(s)$ at the lower wall.
The most important parameter is the definition of the \benennung{objective function} for $\beta(s)$.



\comment{
With this simulation setup the optimisation process is started. 
The first step of the optimisation loop is to ste new boundary points q. 
In the beginning (if there are less then XX in GPinit) the points are taken randomly in the given parameter range.
In the next step the domain is build with the new boundary points and the \name{OpenFOAM} simulation is started. 
When the simulation is finished, the postprocessing is performed to compute the current Clauser parameter $\beta$ and the residual R.
As a last step the files and XX are updated. 
If the optimisation was performed for a certain number of steps the new boundary points q are selected with the bayesian optmization. 
<the optimisation runs until ???XX, sich that the ZPG is ensured.
In the next step the velocity profile for the optimized domain is extracted. 
The values are extracted from the RANS simulation at the wand normal position which corresponds to the topwall of the DNS domain. 
This velocity profile is imposed to the DNS setup and then the DNS simulation is performed. 
}


\subsubsection{Data extraction} \label{subsec:data_extr}

Once the optimisation of the Clauser parameter in \name{OpenFOAM} finished, the next step is to transfer the relevant quantities into the \name{neko} simulation.
As stated before, the domain use for the \acrshort{dns} is less high in wall-normal direction. 
Therefore the shape modification from the \acrshort{rans} simulation is not transferred.
However, only the quantities influenced by the shape adaptation are transferred.
Since the pressure itself is not directly adapted the velocity components build the defining quantities.
The velocity components are extracted at the height where the \acrshort{dns} domain has its upper wall, such that they can be imposed as boundary condition.
The extraction of the data from the \name{OpenFOAM} simulation is done with \name{Python}.
First the simulation with the best optimisation result is detected and stored in a separate directory.
The \name{Python} script reads the coordinates from the \texttt{C/Cx/Cy} files and the velocities from the corresponding fields. 
At a user-defined height the velocity values then are interpolated from the surrounding cells. 
The interpolated velocity are stored in a \texttt{csv} file. 
The integration of these values in the \acrshort{dns} is described in the next section.


\section{Optimised Neko simulation setup}

The setup of the simulations with the optimized boundary conditions at the upper wall have despite from this similar settings as the ones for the curved \acrshort{tbl} described in \autoref{sec:MethodCurvedTBL}.

\paragraph{Velocity integration}

To run the \acrshort{dns} with \acrfull{zpg} the velocities extracted from \name{OpenFOAM} need to be applied as boundary condition on the \name{neko} domain. 
This is implemented in the \texttt{user.f90} file. 
There the boundary points are read from the mesh. 
Then the streamwise position $s$ for the current point is calculated.
With this position the method \texttt{interpolate_velocity(s)} is called.
In this method the velocity is linearly interpolated form the surrounding velocity values read from the \texttt{csv} file.
This is done for all streamwise positions that are included in the \texttt{csv} file. 
Hence, also a part of the straight inflow in front of the curvature, and a part of the tail are included.
In the parts of the domain that are not included, $(1,0,0)$ is applied as before. 
To avoid sharp changes of the boundary condition when changing from $(1,0,0)$ to the optimised velocities, a blending is included.
This blends the edge value of the optimised velocities into the $(1,0,0)$ region in 10 length units in streamwise direction. 


\comment{
\section{General workflow}

In this section the general workflow followed during this project is presented.

XX Hier noch allgemeines Vorgehesn beschreiben sonst zu detailiert im folgenden XX


\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{flowchart_workflow.pdf}
    \caption{Worklow}
    \label{fig:workflow}
\end{figure}

To get familiar with the \name{neko} software an example case of the neko example is simulated. We decided to use the channel case since there are some similarities compared to the TBL (see \autoref{chap:basics}). The obtained simulation results as well as the postprocessing procedure are verified and then validated with reference data. 
In the following step the set up is switched inot a TBL. To do this a new domain with a new mesh is created. At this point a plane TBL is analysed first.
A detailed description of the setup can be found in XX. This setup is validated with reference data of other plane TBL simulations eg XX XX. As with the cahnnel simulations, these simulations are used to validate the simulation setup and the postprocessing procedure. 
IN the final part of the project the curved TBL is analysed. Therefore it is necessary to build a simulation setup where a ZPG is ensured such that only curvature effects can be analysed. To create such a setup multiple steps are necessary which are depicted in \autoref{fig:workflow}. First a working setup of a curved TBL without pressure modification is established in \name{neko} including the domain size, case settings, vector rotations etc. which are descriped in detail at XX. With this setup a simulation is performed. 

- einarbeiten in neko software
--> channel simulations
- perform simulations and verify results with reference data

- create TBL domain 
- perform plane tbl isimulations and validate the results with reference data

- create curved domain 
- set uo neko settings for working simulaiton
- copy setup in OpenFOAM
- run optimisation

- beta (clauser parameter) at the lower wall as target measures
- odjective function: distnace between beta at the bewertungspoints q nad prescribed beta along the lower wall (at the same x position)

- extract results (U, ??) from OpenFOAM
- impose these data as boundary condition at top wall in neko simulation
- perform neko simulation

- do this for different domains: why these domains?
}

